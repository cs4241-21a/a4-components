import React from "react";

const Header = ({ title }) => {
    return (
        <>
            <title>
                {title}
            </title>
        
        </>
    );
};

export default Header;